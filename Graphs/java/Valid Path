Problem Description
There is a rectangle with left bottom as (0, 0) and right up as (x, y).

There are N circles such that their centers are inside the rectangle.

Radius of each circle is R. Now we need to find out if it is possible that we can move from (0, 0) to (x, y) without touching any circle.

Note : We can move from any cell to any of its 8 adjecent neighbours and we cannot move outside the boundary of the rectangle at any point of time.



Problem Constraints
0 <= x , y, R <= 100

1 <= N <= 1000

Center of each circle would lie within the grid



Input Format
1st argument given is an Integer x , denoted by A in input.

2nd argument given is an Integer y, denoted by B in input.

3rd argument given is an Integer N, number of circles, denoted by C in input.

4th argument given is an Integer R, radius of each circle, denoted by D in input.

5th argument given is an Array A of size N, denoted by E in input, where A[i] = x cordinate of ith circle

6th argument given is an Array B of size N, denoted by F in input, where B[i] = y cordinate of ith circle



Output Format
Return YES or NO depending on weather it is possible to reach cell (x,y) or not starting from (0,0).



Example Input
Input 1:

 x = 2
 y = 3
 N = 1
 R = 1
 A = [2]
 B = [3]
Input 2:

 x = 1
 y = 1
 N = 1
 R = 1
 A = [1]
 B = [1]


Example Output
Output 1:

 NO
Output 2:

 NO


Example Explanation
Explanation 1:

 There is NO valid path in this case
Explanation 2:

 There is NO valid path in this case
 
 public class Solution {
    int[][] ml;
    int[][] v;
    int[] a = {-1,-1,-1,0,1,1,1,0};
    int[] b = {-1,0,1,1,1,0,-1,-1};
    int r;
    int row;
    int col;
    public String solve(int A, int B, int C, int D, int[] E, int[] F) {
        ml = new int[A+1][B+1];
        v = new int[A+1][B+1];
        r = D;
         row = A;
         col = B;

        buildformula(E, F);
        //return "YES";
        return visit(0,0) ? "YES" : "NO";
    }

    public void buildformula(int[] E, int[] F)
    {
     for(int i=0;i<=row;i++)
     {
         for(int j=0;j<=col;j++)
         {
             for(int k=0;k<E.length;k++)
             {
                 if(Math.pow((E[k]-i), 2) + Math.pow((F[k]-j), 2) <= r*r)
                 {
                     //System.out.println(i + "-one" + j );
                     ml[i][j] = 1;
                 }
             }
         }
     }   
    }
    public void build(int r, int[] E, int[] F)
    {
        int len = E.length;
        int p =0;

            int x = E[p];
            int y = F[p];

            ml[x][y] = 1;

            for(int i=0;i<a.length;i++)
            {
                int k =1;
                while(k<=r)
                {
                    int m = x + (k*a[i]);
                    int n = y + (k*b[i]);
                    k++;
                    if(m<0 || n<0 || m>row || n>col)
                    {
                        break;
                    }
                    ml[m][n] = 1;
                }
            }


    }

    public boolean visit(int x, int y)
    {
        if(x<0 || y<0 || x>row || y>col)
        {
            return false;
        }
         if(v[x][y] == 1)
        {
            return false;
        }
        //System.out.println(x + "-" + y + "-" + ml[x][y]);
        v[x][y] = 1;

        if(ml[x][y] == 0)
        {
            if(x == row && y == col)
            {
                return true;
            }
            for(int i=0;i<a.length;i++)
            {
               boolean res = visit(x+a[i], y+b[i]);

               if(res)
               {
                   return true;
               }
            }
            
        }
        return false;
    }
}
